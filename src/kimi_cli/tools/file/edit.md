Delete a range of lines from a file and insert new content at the specified position.

**Tips:**
- Only use this tool on text files.
- Line numbers are 1-based.
- To delete lines without inserting anything, set `new_string` to an empty string `""`.
- To insert content without deleting any lines, set `start` and `end` to the same line number.
